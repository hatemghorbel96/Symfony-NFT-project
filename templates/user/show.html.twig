{% extends 'base.html.twig' %}

{% block title %}profile {{user.name}} {{user.lastname}}{% endblock %}

{% block body %}
{# id = {{user.id}}<br>
nom = {{user.email}}<br>
following {{user.follows | length}}<br>
followers {{follwers | length}}<br>
{% if isfollowed %}
<a href="{{path('unfollow',{'id':user.id})}}">unfollow</a>

{% else %}
<a href="{{path('follow',{'id':user.id})}}">follow</a>
{% endif %} #}

<br><br><br><br>
  {% if user.cover %}
  <div class="page-title-overlap pt-4" style="background-image: url('/uploads/{{user.cover}}" > 
  {% else %}
 <div class="page-title-overlap bg-accent pt-4">
  {% endif %}
        <div class="container d-lg-flex justify-content-between py-2 py-lg-3">
          <div class="order-lg-2 mb-3 mb-lg-0 pt-lg-2">
            <nav aria-label="breadcrumb">
             
            </nav>
            
          </div>
          <div class="order-lg-1 pe-lg-4 text-center text-lg-start">
         
          </div>
        </div>
      </div>
      <!-- Page content-->
      <section class="container mb-4">
        <div class="mt-lg-2">
        
        
                {% if user.image %}
                  <img class="rounded-circle border border-3 d-lg-block d-none mt-5" src="/uploads/{{user.image}}" width="160" style="height:140px" alt="Avatar" style="border-color: #ffffff !important;">

                {% else %}
                <img class="rounded-circle border border-3 d-lg-block d-none mt-5" src="{{asset('def.png')}}" width="160" style="height:140px" alt="Avatar" style="border-color: #ffffff !important;">

                          
                  {% endif %}
        
          <div class="d-lg-none d-flex align-items-center justify-content-center"><img class="rounded-circle me-2" src="/uploads/{{user.image}}" width="36" alt="Avatar">
            <h6 class="mb-0 text-light">Robert Fox</h6>
          </div>
        </div> 
        
        
        <div class="row">

        <ol class="breadcrumb breadcrumb-light flex-lg-nowrap justify-content-center justify-content-lg-start mt-3" style="padding-right:250px;">
                
                <li class="breadcrumb-item text-nowrap"><a href="{{path('editprofile')}}" class="btn btn-danger btn-sm ">Edit profile</a>
                </li>
                 <li class="breadcrumb-item text-nowrap"><a href="{{path('editprofile')}}" class="btn btn-danger btn-sm ">Edit cover</a>
                </li>
              
         </ol>
          <!-- Sidebar-->
          <aside class="col-lg-3 pt-lg-4">

          
            <div class="offcanvas offcanvas-collapse" id="vendor-sidebar">
              <div class="offcanvas-header align-items-center shadow-sm">
                <h2 class="h5 mb-0">Vendor</h2>
                <button class="btn-close ms-auto" type="button" data-bs-dismiss="offcanvas" aria-label="Close"></button>
              </div>
              <div class="offcanvas-body">
                <!-- User avatar-->
                
                <img class="rounded-circle d-lg-none d-block mb-4" src="/uploads/{{user.image}}" width="140" alt="Avatar">
                <!-- User meta-->
                <div class="mb-4 pb-2">
                  <h3 class="mb-2 fs-lg fw-normal text-body">{{user.name}} {{user.lastname}}</h3>
                  <h2 class="h4 mb-3">@{{user.name}}_{{user.lastname}}</h2><span class="d-inline-block mb-3 fs-sm text-muted">Joined July 2022</span>
                  <a class="d-block mb-4 text-decoration-none" href="#">
                  <span class="badge bg-primary">{{user.userid}}</span><i class="ci-copy ms-2 ps-1 fs-sm text-accent" data-bs-toggle="tooltip" data-bs-placement="top" title="Copy"></i></a>
                  <div class="d-flex align-items-center flex-wrap text-nowrap mb-4 fs-sm">
                    <div class="mb-2 me-sm-3 me-2 text-muted"><span class='fw-medium text-body'>{{follwers | length}}</span> followers</div>
                    <div class="mb-2 me-sm-3 me-2 ps-sm-3 ps-2 border-start text-muted"><span class='fw-medium text-body'>{{user.follows | length}}</span> following</div>
                  </div>
                  <!-- Follow-->
                  <div class="d-flex flex-lg-row flex-column">
                      {% if app.user %}
                    {% if app.user.id == user.id %}
                    {% else %}
                  {% if isfollowed %}
                    <a class="btn btn-success " href="{{path('unfollow',{'id':user.id})}}"><i class="ci-check-circle"> </i> Following</a>
                    {% else %}
                     <a class="btn btn-accent" href="{{path('follow',{'id':user.id})}}"><i class="ci-add-user pe-1"></i> Follow</a>
                    {% endif %}
                    {% endif %}
                    {% endif %}
                  </div>
                </div>
              </div>
              <div class="offcanvas-footer d-flex flex-column align-items-start p-lg-0">
                <!-- User bio-->
                <div class="mb-4 pb-2 fs-sm">
                  <h6 class="mb-2 fs-md">Short bio</h6>
                  <p class="mb-0">{{user.bio}}</p><a class="d-inline-block mt-1" href="#"><ins>Read more</ins></a>
                </div>
                <!-- Socials-->
                <div class="mt-n2"><a class="btn-social bs-twitter me-2 mt-2" href="{{user.twitter}}"><i class="ci-twitter"></i></a><a class="btn-social bs-facebook me-2 mt-2" href="https://www.facebook.com/{{user.fb}}"><i class="ci-facebook"></i></a><a class="btn-social bs-instagram me-2 mt-2" href="https://www.instagram.com/{{user.insta}}"><i class="ci-instagram"></i></a><a class="btn-social bs-youtube me-2 mt-2" href="#"><i class="ci-youtube"></i></a></div>
              </div>
            </div>
          </aside>
          <!-- Content-->
          <div class="col-lg-9 mt-lg-n5 mt-5 pt-lg-2 pt-3">
            <div class="d-flex flex-md-row flex-column align-items-md-center justify-content-md-between mb-lg-4 mb-3 pb-md-1">
              <!-- Nav tabs-->
              <div class="mb-md-0 mb-4 pb-1" style="overflow-x: auto;">
                <ul class="nav nav-tabs nav-fill flex-nowrap text-nowrap mb-0" role="tablist">
                  <li class="nav-item"><a class="nav-link active" href="#created" data-bs-toggle="tab" role="tab">Created <span class='opacity-60'>({{user.posts | length}})</span></a></li>
                  <li class="nav-item"><a class="nav-link" href="#collections" data-bs-toggle="tab" role="tab">Collections <span class='opacity-60'>({{user.collects | length}})</span></a></li>
                  <li class="nav-item"><a class="nav-link" href="#liked" data-bs-toggle="tab" role="tab">Liked <span class='opacity-60'>({{user.post | length}})</span></a></li>
                  <li class="nav-item"><a class="nav-link" href="#activity" data-bs-toggle="tab" role="tab">Activity</a></li>
                   <li class="nav-item"><a class="nav-link" href="#buy" data-bs-toggle="tab" role="tab">My purchases</a></li>
                </ul>
              </div>
              <!-- Sorting-->
              <div class="d-flex flex-md-row flex-column align-items-md-center flex-shrink-0 ms-md-4 ps-md-2 pb-1">
                <label class="form-label mb-md-0 me-md-2 pe-md-1 fs-sm fw-normal text-nowrap" for="sorting">Sort:</label>
                <select class="form-select" id="sorting" style="min-width: 222px;">
                  <option selected disabled>Newest</option>
                  <option>Oldest</option>
                </select>
              </div>
            </div>
            <!-- Tabs content-->
            <div class="tab-content">
              <!-- Created items grid-->
              <div class="tab-pane fade show active" id="created" role="tabpanel">
                <div class="row row-cols-md-3 row-cols-sm-2 row-cols-1 gy-sm-4 gy-3">
                  <!-- Product-->
        {% for p in user.posts %}
                  <div class="col mb-2">
                    <article class="card h-100 border-0 shadow">
                      <div class="card-img-top position-relative overflow-hidden"><a class="d-block" href="{{path('post_show',{'id':p.id})}}"><img src="/uploads/{{p.img}}" style="min-width:300px; max-height:300px; min-height:300px" alt="Product image"></a>
                        <!-- Countdown timer-->
                          {% if p.minbet %}
                        <div class="badge bg-dark m-3 fs-sm position-absolute top-0 start-0 zindex-5"><i class="ci-time me-1"></i>
                          <div class="countdown d-inline" data-countdown="{{p.datefin | date ('m/d/Y h:m:s') }}">
                           <span class="countdown-days mb-0 me-0">
                            <span class="countdown-value">0</span><span class="countdown-label fs-lg">:</span></span>
                            <span class="countdown-hours mb-0 me-0">
                            <span class="countdown-value">0</span><span class="countdown-label fs-lg">:</span></span>
                            <span class="countdown-minutes mb-0 me-0">
                            <span class="countdown-value">0</span><span class="countdown-label fs-lg">:</span></span>
                            <span class="countdown-seconds mb-0 me-0">
                            <span class="countdown-value">0</span></span></div>
                        </div>
                          {% endif %}
                        <!-- Wishlist button-->
                        <button class="btn-wishlist btn-sm position-absolute top-0 end-0" type="button" data-bs-toggle="tooltip" data-bs-placement="left" title="Add to Favorites" style="margin: 12px;"><i class="ci-heart"></i></button>
                      </div>
                      <div class="card-body">
                        <h3 class="product-title mb-2 fs-base"><a class="d-block text-truncate" href="nft-single-auction-live.html">{{p.title}}</a></h3>
                        
                       

                          {% if p.minbet %}
                          <span class="fs-sm text-muted">Current bid:</span>
                          {% else %}
                          <span class="fs-sm text-muted">Reserve price:</span>
                          {% endif %}

                        <div class="d-flex align-items-center flex-wrap">
                          {% if p.minbet %}
                          <h4 class="mt-1 mb-0 fs-base text-darker">{{p.minbet / 100}} {{p.currencyv}}</h4><span class="mt-1 ms-1 fs-xs text-muted">(≈ $ {{p.minbet * 1155.60}})</span>
                           {% else %}
                           <h4 class="mt-1 mb-0 fs-base text-darker">{{p.price / 100}} {{p.currency}}</h4><span class="mt-1 ms-1 fs-xs text-muted">(≈ $ {{p.price * 1155.60}})</span>
                          {% endif %}
                        </div>
                      </div>
                      <div class="card-footer mt-n1 py-0 border-0">
                        <div class="d-flex align-items-center position-relative mb-1 py-3 border-top"><img class="me-2 rounded-circle" src="/uploads/{{p.user.image}}" width="32" alt="Avatar">
                        <a class="nav-link-style fs-sm stretched-link" href="nft-vendor.html">@{{p.user.name}}_{{p.user.lastname}}</a></div>
                      </div>
                    </article>
                  </div>
                  {% endfor %}
              

                </div>
                <!-- Load more-->
                <button class="btn btn-outline-accent d-block w-100 mt-sm-4 mt-3"><i class="ci-reload me-2"></i>Load more</button>
              </div>
              <!-- Collections-->
              <div class="tab-pane fade" id="collections" role="tabpanel">
                <!-- Collections grid-->
                <div class="row row-cols-md-2 row-cols-1 g-4">
                  
                  {% for c in user.collects %}
                  <div class="col">
                    <article>
                      <div class="card mb-3">
                        <div class="card-body p-3">
                          <!-- Author-->
                       
                          <div class="d-flex align-items-center position-relative mb-3">
                          <img class="rounded-circle me-2" src="/uploads/{{c.user.image}}" width="32" alt="Avatar">
                            <div class="fs-sm">by<a class="ms-1 fw-medium text-accent stretched-link" href="{{path('show_user',{'id':c.user.id})}}">@{{c.user.name}}_{{c.user.lastname}}</a></div>
                          </div>
                          <!-- Collage-->
                            <div class="row row-cols-2 g-2">

                         

                            
                            {% for p in c.posts | slice(0,4) %}
                              <div class="col">

                             
                               <a href="{{path('post_show',{'id':p.id})}}">
                                <img class="rounded-1 mt-2" src="/uploads/{{p.img}}" style="width:150px; height:150px;" alt="Collection item">
                                </a>

                              </div>
                              {% endfor %}

                            </div>
                        </div>
                      </div>
                      <!-- Collection title-->
                      <h3 class="h5 mb-1"><a class="nav-link-style" href="{{path('show_collect',{'id':c.id})}}">{{c.name}}</a></h3><span class="fs-sm text-muted">
                      
                      
                      {% set sum = 0 %}  
                        
                   
                         {% for c in c.posts  %}
                            {% if c.price %}
                            {% set sum = sum + c.price %}
                            {% else %}
                             {% set sum = sum + c.minbet %}
                             {% endif %}
                              {% endfor %}
                   
                               ${{ sum / 100 }}
                     

                      </span>
                    </article>
                  </div>
                 {% endfor %}
                </div>
              </div>
              <!-- Liked-->
              <div class="tab-pane fade" id="liked" role="tabpanel">
                <!-- Liked items grid-->
                <div class="row row-cols-md-3 row-cols-sm-2 row-cols-1 gy-sm-4 gy-3">

               
                 {% for p in user.post %}
                  <!-- Product-->
                  <div class="col mb-2">
                   <article class="card h-100 border-0 shadow">
              <div class="card-img-top position-relative overflow-hidden"><a class="d-block" href="{{path('post_show',{'id':p.id})}}"><img src="/uploads/{{p.img}}" alt="Product image"></a>
                <!-- Countdown timer-->
                 {% if p.minbet %}
                <div class="badge bg-dark m-3 fs-sm position-absolute top-0 start-0 zindex-5"><i class="ci-time me-1"></i>
                  <div class="countdown d-inline"  {# data-countdown="07/22/2022"  #}  data-countdown="{{p.datefin | date ('m/d/Y h:m:s') }}" >
                   <span class="countdown-days mb-0 me-0">
                  <span class="countdown-value">0</span><span class="countdown-label fs-lg">:</span></span>
                  <span class="countdown-hours mb-0 me-0">
                  <span class="countdown-value">0</span><span class="countdown-label fs-lg">:</span></span>
                  <span class="countdown-minutes mb-0 me-0">
                  <span class="countdown-value">0</span><span class="countdown-label fs-lg">:</span></span>
                  <span class="countdown-seconds mb-0 me-0">
                  <span class="countdown-value">0</span></span></div>
                </div>
                {% endif %}
                <!-- Wishlist button-->
                 {% if app.user %}
                 
                 
                  	{% for f in p.likes %}
													{% if f.id == app.user.id %}
											<a class="btn-wishlist btn-sm position-absolute top-0 end-0" href="{{path('down_like',{'id':p.id})}}" data-bs-toggle="tooltip" data-bs-placement="left" title="remove from Favorites" style="margin: 12px;"> <i class="ci-heart-filled" style="color:red"></i></a>
											{% else %}
		                <a class="btn-wishlist btn-sm position-absolute top-0 end-0" href="{{path('up_like',{'id':p.id})}}" data-bs-toggle="tooltip" data-bs-placement="left" title="Add to Favorites" style="margin: 12px;"> <i class="ci-heart"></i></a>	
                    	{% endif %}
												{% else %}
		               <a class="btn-wishlist btn-sm position-absolute top-0 end-0" href="{{path('up_like',{'id':p.id})}}" data-bs-toggle="tooltip" data-bs-placement="left" title="Add to Favorites" style="margin: 12px;"> <i class="ci-heart"></i></a>											
                   	{% endfor %}
									 {% else %}
                   <button class="btn-wishlist btn-sm position-absolute top-0 end-0" href="{{path('app_login')}}" data-bs-toggle="tooltip" data-bs-placement="left" title="Add to Favorites" style="margin: 12px;"> <i class="ci-heart" disabled="disabled"></i></button>	
							
									 {% endif %}  
         
             
             
              </div>
              <div class="card-body">
                <h3 class="product-title mb-2 fs-base"><a class="d-block text-truncate" href="nft-single-auction-live.html">{{p.title}}</a></h3>
                {% if p.minbet %}
                <span class="fs-sm text-muted">Current bid:</span>
                {% else %}
                <span class="fs-sm text-muted">Reserve price:</span>
                {% endif %}
                <div class="d-flex align-items-center flex-wrap">

                {% if p.minbet %}
                  <h4 class="mt-1 mb-0 fs-base text-darker">{{p.minbet / 100}} {{p.currencyv}}</h4><span class="mt-1 ms-1 fs-xs text-muted">(≈ $ {{p.minbet * 1155.60}})</span>
                 {% else %}
                  <h4 class="mt-1 mb-0 fs-base text-darker">{{p.price / 100}} {{p.currency}}</h4><span class="mt-1 ms-1 fs-xs text-muted">(≈ $ {{p.price * 1155.60}})</span>
                  {% endif %}
                </div>
              </div>
              <div class="card-footer mt-n1 py-0 border-0">
                <div class="d-flex align-items-center position-relative mb-1 py-3 border-top">
                <img class="me-2 rounded-circle" src="/uploads/{{p.user.image}}" width="32" alt="img">
                <a class="nav-link-style fs-sm stretched-link" href="nft-vendor.html">@{{p.user.name}}_{{p.user.lastname}}</a></div>
              </div>
            </article>
          </div>
          

                  {% endfor %}  
                  
                
             
                </div>
              </div>
              <!-- Activity-->
              <div class="tab-pane fade" id="activity" role="tabpanel">
                <!-- Orders list-->
                <div class="table-responsive fs-md mb-4 pb-1">
                  <table class="table mb-0" style="min-width: 880px;">
                    <thead class="text-uppercase bg-secondary">
                      <tr>
                        <th>Item</th>
                        <th>Type</th>
                        <th>Price</th>
                        <th>From</th>
                        <th>To</th>
                        <th>Date</th>
                      </tr>
                    </thead>
                    <tbody>
                    {% for a in activity |sort((b, a) => a.id <=> b.id) %}
                      <tr>
                        <td class="py-4">
                          <div class="d-flex align-items-center position-relative"><img class="rounded me-2" src="/uploads/{{a.post.img}}" width="48" alt="Product thumbnail">
                            <div class="ps-1">
                              <h6 class="mb-0 fs-base text-body"><a class="nav-link-style d-block stretched-link" href="#">{{a.post.title}}</a></h6>
                              <span class="fs-sm text-muted">by @{{a.post.user.name}}_{{a.post.user.lastname}}</span>
                            </div>
                          </div>
                        </td>
                        <td class="py-4">
                        {% if a.type == 'bets' %}
                        <span class="badge bg-danger">{{a.type}}</span>
                        {% elseif a.type == 'sales' %}
                         <span class="badge bg-success">{{a.type}}</span>
                         {% elseif a.type == 'won bet' %}
                          <span class="badge bg-warning">{{a.type}}</span>
                        {% endif %}
                        </td>
                        <td class="py-4">
                          <h6 class="mb-1 fs-sm text-darker">{{a.price / 100}} ETH</h6><span class="fs-sm text-muted">(≈ $ {{a.price * 1155.60}})</span>
                        </td>
                        {% for u in users %}
                        {% if u.id == a.fromm %}
                        <td class="py-4"><a class="nav-link-style fw-medium" href="#">@{{u.name}}_{{u.lastname}}</a></td>
                        {% endif %}
                        {% endfor %}
                         {% for u in users %}
                         {% if u.id == a.too %}
                        <td class="py-4"><a class="nav-link-style fw-medium" href="#">@{{u.name}}_{{u.lastname}}</a></td>
                        {% endif %}
                        {% endfor %}
                        <td class="py-4"><span class="fs-sm text-muted">

                        {# endDate and startDate are strings or DateTime objects #}
                       {#    {% set difference = ("now -1 hour"|date("m/d/Y H:i:s", "Europe/Rome")).diff(a.createdAt|date("m/d/Y H:i:s", "Europe/Rome")) %}
                          {% set leftDays = difference.days %}
                          {% if leftDays == 1 %}
                            1 day
                          {% else %}
                            {{ leftDays }} days
                          {% endif %} #}
                   {#   {% set difference = date(a.createdAt|date("m/d/Y", "Europe/Rome")).diff(date('now -1 hour'|date("m/d/Y", "Europe/Rome"))) %}  #}
                  {% set difference = date(a.createdAt|date('now -1 hour'|date("m/d/Y H:i:s", "Europe/Rome"))).diff(date(a.createdAt|date("m/d/Y H:i:s", "Europe/Rome"))) %} 
                      {% set leftDays = difference.days %}
                      {% if leftDays == 1 %}
                        1 day
                      {% else %}
                        {{ leftDays }} days
                      {% endif %} 

                     
                      
                        
                        </span></td>
                      </tr>
                      {% endfor %}
                    </tbody>
                  </table>
                </div>
                <button class="btn btn-outline-accent d-block w-100"><i class="ci-reload me-2"></i>Load more</button>
              </div>



                <!-- bought-->
                 <div class="tab-pane fade" id="buy" role="tabpanel">
                <!-- Liked items grid-->
                <div class="row row-cols-md-3 row-cols-sm-2 row-cols-1 gy-sm-4 gy-3">

                {% if posts %}
                 {% for p in posts %}
                  <!-- Product-->
                  <div class="col mb-2">
                   <article class="card h-100 border-0 shadow">
              <div class="card-img-top position-relative overflow-hidden"><a class="d-block" href="{{path('post_show',{'id':p.id})}}"><img src="/uploads/{{p.img}}" alt="Product image"></a>
                <!-- Countdown timer-->
                 {% if p.minbet %}
                <div class="badge bg-dark m-3 fs-sm position-absolute top-0 start-0 zindex-5"><i class="ci-time me-1"></i>
                  <div class="countdown d-inline"  {# data-countdown="07/22/2022"  #}  data-countdown="{{p.datefin | date ('m/d/Y h:m:s') }}" >
                   <span class="countdown-days mb-0 me-0">
                  <span class="countdown-value">0</span><span class="countdown-label fs-lg">:</span></span>
                  <span class="countdown-hours mb-0 me-0">
                  <span class="countdown-value">0</span><span class="countdown-label fs-lg">:</span></span>
                  <span class="countdown-minutes mb-0 me-0">
                  <span class="countdown-value">0</span><span class="countdown-label fs-lg">:</span></span>
                  <span class="countdown-seconds mb-0 me-0">
                  <span class="countdown-value">0</span></span></div>
                </div>
                {% endif %}
                <!-- Wishlist button-->
                 {% if app.user %}
                 
                 
                  	{% for f in p.likes %}
													{% if f.id == app.user.id %}
											<a class="btn-wishlist btn-sm position-absolute top-0 end-0" href="{{path('down_like',{'id':p.id})}}" data-bs-toggle="tooltip" data-bs-placement="left" title="remove from Favorites" style="margin: 12px;"> <i class="ci-heart-filled" style="color:red"></i></a>
											{% else %}
		                <a class="btn-wishlist btn-sm position-absolute top-0 end-0" href="{{path('up_like',{'id':p.id})}}" data-bs-toggle="tooltip" data-bs-placement="left" title="Add to Favorites" style="margin: 12px;"> <i class="ci-heart"></i></a>	
                    	{% endif %}
												{% else %}
		               <a class="btn-wishlist btn-sm position-absolute top-0 end-0" href="{{path('up_like',{'id':p.id})}}" data-bs-toggle="tooltip" data-bs-placement="left" title="Add to Favorites" style="margin: 12px;"> <i class="ci-heart"></i></a>											
                   	{% endfor %}
									 {% else %}
                   <button class="btn-wishlist btn-sm position-absolute top-0 end-0" href="{{path('app_login')}}" data-bs-toggle="tooltip" data-bs-placement="left" title="Add to Favorites" style="margin: 12px;"> <i class="ci-heart" disabled="disabled"></i></button>	
							
									 {% endif %}  
         
             
             
              </div>
              <div class="card-body">
                <h3 class="product-title mb-2 fs-base"><a class="d-block text-truncate" href="nft-single-auction-live.html">{{p.title}}</a></h3>
                {% if p.minbet %}
                <span class="fs-sm text-muted">Current bid:</span>
                {% else %}
                <span class="fs-sm text-muted">Reserve price:</span>
                {% endif %}
                <div class="d-flex align-items-center flex-wrap">

                {% if p.minbet %}
                  <h4 class="mt-1 mb-0 fs-base text-darker">{{p.minbet / 100}} {{p.currencyv}}</h4><span class="mt-1 ms-1 fs-xs text-muted">(≈ $ {{p.minbet * 1155.60}})</span>
                 {% else %}
                  <h4 class="mt-1 mb-0 fs-base text-darker">{{p.price / 100}} {{p.currency}}</h4><span class="mt-1 ms-1 fs-xs text-muted">(≈ $ {{p.price * 1155.60}})</span>
                  {% endif %}
                </div>
              </div>
              <div class="card-footer mt-n1 py-0 border-0">
                <div class="d-flex align-items-center position-relative mb-1 py-3 border-top">
                <img class="me-2 rounded-circle" src="/uploads/{{p.user.image}}" width="32" alt="img">
                <a class="nav-link-style fs-sm stretched-link" href="nft-vendor.html">@{{p.user.name}}_{{p.user.lastname}}</a></div>
              </div>
            </article>
          </div>
                  {% endfor %}
                  {% else %}
                  <div class="row"></div>
                    
                    <div class="alert alert-danger">{{user.name}} {{user.lastname}} didn't buy anything</div></div>
                   
                  {% endif %}
             
                </div>
              </div>



            </div>
          </div>
        </div>
      </section>
   
    </main>
   
{% endblock %}
