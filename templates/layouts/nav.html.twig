  <header class="navbar d-block navbar-sticky navbar-expand-lg navbar-light position-absolute w-100">
        <div class="container"><a class="navbar-brand d-none d-sm-block me-4 order-lg-1" href="{{path('app_home')}}"><img src="{{asset('hg-dev.png')}}" width="142" alt="Cartzilla"></a>
        <a class="navbar-brand d-sm-none me-2 order-lg-1" href="index.html"><img src="img/logo-icon.png" width="74" alt="Cartzilla"></a>
          <div class="navbar-toolbar d-flex align-items-center order-lg-3">
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarCollapse"><span class="navbar-toggler-icon"></span></button><a class="navbar-tool d-none d-lg-flex" href="javascript:void(0)" data-bs-toggle="collapse" data-bs-target="#searchBox" role="button" aria-expanded="false" aria-controls="searchBox"><span class="navbar-tool-tooltip">Search</span>
            
            {% if app.user %}
             <div class="dropdown">
              {% if app.user.image %}
              <a  data-bs-toggle="dropdown"><img src="/uploads/{{app.user.image}}" class="rounded-circle" width="36" alt="User"></a>
              {% else %}
              <a  data-bs-toggle="dropdown"><img src="{{asset('def.png')}}" class="rounded-circle" width="36" alt="User"></a>
              {% endif %}
                <ul class="dropdown-menu">
                 <div style="min-width: 14rem;">
                  <h6 class="dropdown-header">@{{app.user.name}}_{{app.user.lastname}}</h6>
                  <a class="dropdown-item d-flex align-items-center" href="{{path('profile')}}"><i class="ci-user opacity-60 me-2"></i>profile</a>
                  <a class="dropdown-item d-flex align-items-center" href="{{path('editprofile')}}"><i class="ci-settings opacity-60 me-2"></i>Profile Settings</a>
                  <a class="dropdown-item d-flex align-items-center" href="{{path('showitem')}}"><i class="ci-image opacity-60 me-2"></i>My Items</a>
                  <a class="dropdown-item d-flex align-items-center" href="{{path('showcollection')}}"><i class="ci-view-list opacity-60 me-2"></i>My Collections</a>
                  <a class="dropdown-item d-flex align-items-center" href="{{path('app_logout')}}"><i class="ci-sign-out opacity-60 me-2"></i>Logout</a>
                </div>
                </ul>
              </div>
              {% else %}

             {#  <div class="navbar-tool-icon-box"><i class="navbar-tool-icon ci-search"></i></div></a><a class="navbar-tool ms-lg-2"  href="#"><i class="navbar-tool-icon ci-user"></i>
             <a class="navbar-tool ms-lg-2" href="#" ><i class="navbar-tool-icon ci-user"></i> #}

             <div class="navbar-tool-icon-box">
               <a  href="{{path('app_login')}}" >
             <i class="navbar-tool-icon ci-user"></i>
              </a>
             </div>

             <div class="navbar-tool-icon-box">
              <a  href="{{path('app_register')}}" >
             <i class="navbar-tool-icon ci-add-user"></i>
               </a>
             </div>

            
              {% endif %}
                <a class="btn btn-sm btn-accent rounded-1 ms-lg-4 ms-2" href="nft-connect-wallet.html">Connect Wallet</a>
          </div>
          <div class="collapse navbar-collapse me-auto order-lg-2" id="navbarCollapse">
            <!-- Search (mobile)-->
            <div class="d-lg-none py-3">
              <div class="input-group"><i class="ci-search position-absolute top-50 start-0 translate-middle-y ms-3"></i>
                <input class="form-control rounded-start" type="text" placeholder="What do you need?">
              </div>
            </div>
            <!-- Primary menu-->
            <ul class="navbar-nav">
              <li class="nav-item "><a class="nav-link" href="{{path('app_home')}}">Home</a></li>
              <li class="nav-item "><a class="nav-link" href="{{path('categories')}}">Categories</a></li>
               <li class="nav-item "><a class="nav-link" href="{{path('posts')}}">All NFTs</a></li>
                <li class="nav-item "><a class="nav-link" href="{{path('collections')}}">Collections</a></li>
           {% if app.user %}
              <li class="nav-item "><a class="nav-link" href="{{path('add_post')}}">Create ur NFTs</a></li>
             <li class="nav-item "><a class="nav-link" href="{{path('add_collect')}}">Add Collection</a></li>
              {% endif %}
             
    
            </ul>

           

          </div>
        </div>
        <!-- Search collapse-->
        <div class="search-box collapse" id="searchBox">
          <div class="container py-2">
            <div class="input-group"><i class="ci-search position-absolute top-50 start-0 translate-middle-y ms-3"></i>
              <input class="form-control rounded-start" type="text" placeholder="What do you need?">
            </div>
          </div>
        </div>
      </header>
      <!-- Hero-->